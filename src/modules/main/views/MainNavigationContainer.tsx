import { createAppContainer } from 'react-navigation';

import { MainScreens } from '../navigation/mainScreens';

const MainNavigationContainer = createAppContainer(MainScreens);

export default MainNavigationContainer;
